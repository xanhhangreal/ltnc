#include <bits/stdc++.h>

using namespace std;
const int maxn = 1e6 + 5;
string s;
set < string > sett;
int main(){
    cin >> s;
    int n = s.size();
    for(int i = 0; i < n; i++)
        for(int j = i + 1; j < n; j++) {
            string
        }
}
